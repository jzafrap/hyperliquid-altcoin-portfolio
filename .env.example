# Hyperliquid network for the app. Defaults to "testnet" if unset (testnet-first).
# Set to "mainnet" ONLY when the flow has been validated on testnet.
VITE_HL_NETWORK=testnet

# Optional: WalletConnect project id, if a WalletConnect connector is added later.
# VITE_WALLETCONNECT_PROJECT_ID=
